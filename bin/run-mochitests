#!/usr/bin/env bash

ROOT=`dirname $0`
FIREFOX_PATH="$ROOT/../firefox"

if [ ! -d firefox ]; then
  "$ROOT/download-firefox-artifact"
fi

"$ROOT/make-firefox-bundle" "$FIREFOX_PATH"
cd "$FIREFOX_PATH"
./mach build
./mach mochitest --subsuite devtools devtools/client/debugger/new/test/mochitest/